# SSH MCP Server Configuration

# Connection settings
SSH_CONNECT_TIMEOUT=30
SSH_COMMAND_TIMEOUT=180
SSH_INACTIVITY_TIMEOUT=300
SSH_MAX_RETRIES=3
SSH_RETRY_DELAY_MS=1000
SSH_COMPRESSION=true

# Server settings (ssh-mcp HTTP binary only)
MCP_PORT=8000

# Logging
RUST_LOG=info
