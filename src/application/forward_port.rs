//! Use case stub — implementation lands in H15.
//!
//! Feature-gated by `port_forward`. Scaffold reserves the module path so
//! parallel agents can populate it without racing on `mod.rs`.

#![allow(dead_code, reason = "scaffold awaiting implementation in H15")]
#![cfg(feature = "port_forward")]

/// Placeholder marker. Replaced by the real use case struct in H15.
#[derive(Debug, Default, Clone, Copy)]
pub struct Placeholder;
